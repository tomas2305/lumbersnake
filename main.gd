extends Node2D

#@export var tree_scene: PackedScene
#
#
#func _ready() -> void:
	#for n in 5:
		#spawn_random_tree()
#func spawn_random_tree():
	#var tree = tree_scene.instantiate()  # Instancia anónima
	#tree.position = Vector2(randi_range(100, 900), randi_range(100, 500))
	#tree.add_to_group("trees")  # Asegúrate de agregarlo al grupo
	#add_child(tree)
